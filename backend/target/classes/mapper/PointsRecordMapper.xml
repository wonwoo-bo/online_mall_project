<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.module.user.mapper.PointsRecordMapper">

    <insert id="insert" parameterType="com.mall.module.user.entity.PointsRecord">
        INSERT INTO points_record (user_id, points, type, description, create_time)
        VALUES (#{userId}, #{points}, #{type}, #{description}, #{createTime})
    </insert>

    <select id="selectByUserId" resultType="com.mall.module.user.entity.PointsRecord">
        SELECT * FROM points_record WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

</mapper>
