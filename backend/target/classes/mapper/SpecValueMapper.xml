<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.module.product.mapper.SpecValueMapper">

    <insert id="insert" parameterType="com.mall.module.product.entity.SpecValue">
        INSERT INTO spec_value (merchant_id, type_id, value, sort_order, status, create_time, update_time)
        VALUES (#{merchantId}, #{typeId}, #{value}, #{sortOrder}, #{status}, NOW(), NOW())
    </insert>

    <update id="updateById" parameterType="com.mall.module.product.entity.SpecValue">
        UPDATE spec_value 
        SET value = #{value}, sort_order = #{sortOrder}, status = #{status}, update_time = NOW()
        WHERE id = #{id} AND merchant_id = #{merchantId}
    </update>

    <delete id="deleteById">
        DELETE FROM spec_value WHERE id = #{id} AND merchant_id = #{merchantId}
    </delete>

    <delete id="deleteByTypeId">
        DELETE FROM spec_value WHERE type_id = #{typeId} AND merchant_id = #{merchantId}
    </delete>

    <select id="selectById" resultType="com.mall.module.product.entity.SpecValue">
        SELECT * FROM spec_value WHERE id = #{id} AND merchant_id = #{merchantId}
    </select>

    <select id="selectByTypeId" resultType="com.mall.module.product.entity.SpecValue">
        SELECT * FROM spec_value WHERE merchant_id = #{merchantId} AND type_id = #{typeId} AND status = 1 ORDER BY sort_order ASC
    </select>

    <select id="selectByMerchantId" resultType="com.mall.module.product.entity.SpecValue">
        SELECT * FROM spec_value WHERE merchant_id = #{merchantId} AND status = 1 ORDER BY type_id ASC, sort_order ASC
    </select>

    <select id="checkValueExists" resultType="Integer">
        SELECT COUNT(*) FROM spec_value WHERE merchant_id = #{merchantId} AND type_id = #{typeId} AND value = #{value}
        <if test="excludeId != null">AND id != #{excludeId}</if>
    </select>

</mapper>