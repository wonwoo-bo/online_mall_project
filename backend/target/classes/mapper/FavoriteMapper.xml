<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.module.product.mapper.FavoriteMapper">

    <resultMap id="favoriteResultMap" type="com.mall.module.product.entity.Favorite">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="product_id" property="productId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectByUserId" resultMap="favoriteResultMap">
        SELECT * FROM favorite WHERE user_id = #{userId} ORDER BY create_time DESC LIMIT #{offset}, #{limit}
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM favorite WHERE user_id = #{userId}
    </select>

    <select id="exists" resultType="int">
        SELECT COUNT(*) FROM favorite WHERE user_id = #{userId} AND product_id = #{productId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO favorite (user_id, product_id)
        VALUES (#{userId}, #{productId})
    </insert>

    <delete id="delete">
        DELETE FROM favorite WHERE user_id = #{userId} AND product_id = #{productId}
    </delete>

</mapper>
