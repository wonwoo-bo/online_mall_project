<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.module.product.mapper.ProductImageMapper">

    <resultMap id="productImageResultMap" type="com.mall.module.product.entity.ProductImage">
        <id column="id" property="id"/>
        <result column="product_id" property="productId"/>
        <result column="image_url" property="imageUrl"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="is_main" property="isMain"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectByProductId" resultMap="productImageResultMap">
        SELECT * FROM product_image WHERE product_id = #{productId} ORDER BY sort_order ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_image (product_id, image_url, sort_order, is_main)
        VALUES (#{productId}, #{imageUrl}, #{sortOrder}, #{isMain})
    </insert>

    <delete id="deleteByProductId">
        DELETE FROM product_image WHERE product_id = #{productId}
    </delete>

</mapper>
