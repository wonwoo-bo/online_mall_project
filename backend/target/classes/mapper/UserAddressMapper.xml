<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mall.module.user.mapper.UserAddressMapper">

    <resultMap id="BaseResultMap" type="com.mall.module.user.entity.UserAddress">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="receiver_name" property="receiverName"/>
        <result column="receiver_phone" property="receiverPhone"/>
        <result column="province" property="province"/>
        <result column="city" property="city"/>
        <result column="district" property="district"/>
        <result column="detail_address" property="detailAddress"/>
        <result column="is_default" property="isDefault"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_address (user_id, receiver_name, receiver_phone, province, city, district, detail_address, is_default, create_time, update_time)
        VALUES (#{userId}, #{receiverName}, #{receiverPhone}, #{province}, #{city}, #{district}, #{detailAddress}, #{isDefault}, NOW(), NOW())
    </insert>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT id, user_id, receiver_name, receiver_phone, province, city, district, detail_address, is_default, create_time, update_time
        FROM user_address
        WHERE user_id = #{userId}
        ORDER BY is_default DESC, create_time DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, user_id, receiver_name, receiver_phone, province, city, district, detail_address, is_default, create_time, update_time
        FROM user_address
        WHERE id = #{id}
    </select>

    <update id="update">
        UPDATE user_address
        <set>
            <if test="receiverName != null">receiver_name = #{receiverName},</if>
            <if test="receiverPhone != null">receiver_phone = #{receiverPhone},</if>
            <if test="province != null">province = #{province},</if>
            <if test="city != null">city = #{city},</if>
            <if test="district != null">district = #{district},</if>
            <if test="detailAddress != null">detail_address = #{detailAddress},</if>
            <if test="isDefault != null">is_default = #{isDefault},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM user_address WHERE id = #{id}
    </delete>

    <update id="clearDefault">
        UPDATE user_address SET is_default = 0 WHERE user_id = #{userId}
    </update>

    <update id="setDefault">
        UPDATE user_address SET is_default = #{isDefault} WHERE id = #{id}
    </update>

</mapper>