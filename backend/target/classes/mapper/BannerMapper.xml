<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.module.admin.mapper.BannerMapper">

    <select id="selectAll" resultType="com.mall.module.admin.entity.Banner">
        SELECT * FROM banner ORDER BY sort_order ASC, id DESC
    </select>

    <select id="selectActive" resultType="com.mall.module.admin.entity.Banner">
        SELECT * FROM banner WHERE status = 1 ORDER BY sort_order ASC, id DESC
    </select>

    <select id="selectById" resultType="com.mall.module.admin.entity.Banner">
        SELECT * FROM banner WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO banner (title, description, image_url, link_url, color, sort_order, status)
        VALUES (#{title}, #{description}, #{imageUrl}, #{linkUrl}, #{color}, #{sortOrder}, #{status})
    </insert>

    <update id="update">
        UPDATE banner SET
        <trim suffixOverrides=",">
            <if test="title != null">title = #{title},</if>
            <if test="description != null">description = #{description},</if>
            <if test="imageUrl != null">image_url = #{imageUrl},</if>
            <if test="linkUrl != null">link_url = #{linkUrl},</if>
            <if test="color != null">color = #{color},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
        </trim>
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM banner WHERE id = #{id}
    </delete>

</mapper>
