<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.module.product.mapper.SpecTypeMapper">

    <insert id="insert" parameterType="com.mall.module.product.entity.SpecType" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO spec_type (merchant_id, name, sort_order, status, create_time, update_time)
        VALUES (#{merchantId}, #{name}, #{sortOrder}, #{status}, NOW(), NOW())
    </insert>

    <update id="updateById" parameterType="com.mall.module.product.entity.SpecType">
        UPDATE spec_type 
        SET name = #{name}, sort_order = #{sortOrder}, status = #{status}, update_time = NOW()
        WHERE id = #{id} AND merchant_id = #{merchantId}
    </update>

    <delete id="deleteById">
        DELETE FROM spec_type WHERE id = #{id} AND merchant_id = #{merchantId}
    </delete>

    <select id="selectById" resultType="com.mall.module.product.entity.SpecType">
        SELECT * FROM spec_type WHERE id = #{id} AND merchant_id = #{merchantId}
    </select>

    <select id="selectByMerchantId" resultType="com.mall.module.product.entity.SpecType">
        SELECT * FROM spec_type WHERE merchant_id = #{merchantId} ORDER BY sort_order ASC, create_time DESC
    </select>

    <select id="checkNameExists" resultType="Integer">
        SELECT COUNT(*) FROM spec_type WHERE merchant_id = #{merchantId} AND name = #{name}
        <if test="excludeId != null">AND id != #{excludeId}</if>
    </select>

    <update id="moveToRecycle">
        UPDATE spec_type SET status = -1, delete_time = NOW(), update_time = NOW() WHERE id = #{id} AND merchant_id = #{merchantId}
    </update>

    <select id="selectRecycleList" resultType="java.util.Map">
        SELECT id, name, delete_time AS deleteTime
        FROM spec_type 
        WHERE merchant_id = #{merchantId} AND status = -1
        ORDER BY delete_time DESC
    </select>

    <update id="restoreSpecType">
        UPDATE spec_type SET status = 1, delete_time = NULL, update_time = NOW() WHERE id = #{id} AND merchant_id = #{merchantId}
    </update>

    <delete id="forceDeleteSpecType">
        DELETE FROM spec_type WHERE id = #{id} AND merchant_id = #{merchantId}
    </delete>

</mapper>