<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.module.product.mapper.ReviewLikeMapper">

    <select id="existsByReviewIdAndUserId" resultType="int">
        SELECT COUNT(1) FROM review_like WHERE review_id = #{reviewId} AND user_id = #{userId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO review_like (review_id, user_id, create_time)
        VALUES (#{reviewId}, #{userId}, #{createTime})
    </insert>

</mapper>
