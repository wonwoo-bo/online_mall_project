#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../bin/vue-demi-switch.js" "$@"
else
  exec node  "$basedir/../../bin/vue-demi-switch.js" "$@"
fi
# cmd-shim-target=D:/version_end/merged-project/frontend/node_modules/.pnpm/vue-demi@0.14.10_vue@3.5.34/node_modules/vue-demi/bin/vue-demi-switch.js
